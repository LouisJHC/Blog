<% @posts.each do |p| %>
    <div class= "wrappingpost">
        
        <h1 class="title"><%= link_to p.title, p %></h1>
        <p class="date"><%= p.created_at.strftime("%B, %d, %Y") %></p>
    </div>

<% end %>